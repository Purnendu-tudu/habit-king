import { View, Text } from "react-native";
import React from "react";

const NotFoundPage = () => {
  return (
    <View>
      <Text>Sorry No Router Exists on that name</Text>
    </View>
  );
};

export default NotFoundPage;
